import { Text, View, SafeAreaView, Image, StatusBar, FlatList } from 'react-native';
import { COLORS, SIZES, SHADOWS, FONTS, asstes } from '../constants';
import { CircleButton, RectButton, SubInfo, FocusStatusBar, DetailsDesc, DetailsBid } from '../components';

const Details = ({ route, navigation }) => {
  return (
    <Text>Details</Text>
  )
}

export default Details